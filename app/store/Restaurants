Ext.create('Ext.data.Store', {
    model: 'Restaurant',
    sorters: ['distance', 'real_name'],
    proxy: {
        type: 'ajax',
        url : 'get_json.php',
        reader: 'json'
    },
    autoLoad: true
});